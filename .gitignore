# 중간 산출물 / 최종 결과물 (재생성 가능)
data/
output/

# 파이썬
.venv/
__pycache__/
*.pyc
*.pyo

# 환경변수 (토큰 포함) — 절대 커밋 금지
.env
.env.local

# Flet/빌드 산출물
/build/
/dist/

# 에이전트·도구 상태 (프로젝트 산출물 아님)
.claude/
.omc/

# 참고: uv.lock 은 재현성을 위해 커밋한다(무시하지 않음).
